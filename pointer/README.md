# Memory Explorer

Practice project to understand pointers and memory in C++.

## What it does
- Creates a variable and a pointer to it
- Prints value, memory address, and pointer address
- Modifies value through pointer

## Concepts covered
- Pointers (`int*`)
- Address-of operator (`&`)
- Dereference operator (`*`)
- Memory addresses